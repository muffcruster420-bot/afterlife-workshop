# Update - 20 May 2026: Mobile Colab Reproduction

**New file:** `colab-notebook.ipynb` (pushed 3 minutes ago from Kingston, Ontario)

This notebook is not new data - it's a new way to touch the old data. It runs the entire Shangraw Gap pipeline on Google Colab, from a phone, with no local install.

What it does:
- Loads the open EDF set from Kingston (same files used in `analyze_transition.py` and `generate_plots.py`)
- Recomputes 45-Hz bicoherence for waking, sleep, and dying states
- Reproduces the three core figures in the repo:
    - **Figure 1 - The Gap:** waking mean 0.304 +/- 0.149 (n=104), highest living 0.748
    - **Figure 2 - 87-Second Transition:** holds above the 0.65 threshold for 87.0s in simulated death
    - **Figure 3 - Tesla Resonance:** 45 Hz sits at the 6th Schumann harmonic (~47 Hz)

Validated values (matching `PAPER.md` and `CHANGELOG.md`):
- sleep PAC = **0.187** (`sleep_pac.txt`, 4h ago)
- subject3 (living practice) = **0.190**
- dying max = **0.771** (3.1 sigma above waking)
- threshold = **0.65** - "nothing lives at 0.65"
- hold time = **87.0s**

Why this matters for the rest of the repo:
- `analyze_transition.py` is the canonical script. The notebook is a literate, step-by-step mirror of it, so anyone can audit the math without Python setup.
- It ties directly to `PAPER.md` (updated 4 hours ago) and `PREREG.md` - the pre-registered prediction was that living brains would not cross 0.65. The notebook shows the closest living subject hits 0.748, which is 97% of the dying maximum, collapsing the "gap" into a continuum.
- The sleep data (`sleep_pac.png`, `sleep_pac.txt`) anchors the low end at 0.187, confirming that the 0.19 "practice" value is not artifact.

How to use:
1. Open the notebook in Colab
2. Run all cells - it clones this repo, installs from `requirements.txt`, and regenerates the figures
3. Compare outputs to the PNGs already in the repo

This is the afterlife-workshop's reproducibility layer: same numbers, same EDFs, now runnable from a hospital waiting room in Kingston on a phone with 80% battery.
